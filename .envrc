# Claude Code auto-continue configuration for ichizen project
# This file is automatically loaded by direnv when entering the project directory

# Source project-specific Claude configuration
source_env .claude-project-config.sh

# Set project environment variables
export CLAUDE_PROJECT="ichizen"
export CLAUDE_AUTO_CONTINUE="true"

# Add project-specific PATH if needed
# export PATH="$PWD/bin:$PATH"